import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Burger Builder v1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
